/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package juegoiavj.math;

/**
 *
 * @author Samuel
 */
public class Recta {
    private double A;
    private double B;
    private double C;
    
    public Recta(Vector v1, Vector v2){
        // De la ecuacion continua de una recta formada por dos vectores
        // sacamos su ecuación general
        A = v2.getY()-v1.getY();
        B = -(v2.getX()-v1.getX());
        C = (-v1.getX())*A + (-v1.getY())*B;
    }
    
    public Vector getPuntoInterseccionPerpendicular(Vector p){
        // Calculamos la pendiente de la recta perpendicular
        double m = B/A;
        // Y la ajustamos para que pase por p
        //double Ap = 
        
        return p;
    }
}
